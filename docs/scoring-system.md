# Scoring System v4 — Two-Coordinate KTC Arbitrage

Chicago Dynasty (Sleeper league `1312124603224555520`, 12-team 1QB dynasty, full PPR; my team: bengramling, roster_id 4). This spec supersedes v2 (never implemented) and v1 (the running engine at the time of writing). It is THE contract: the engine implements it exactly, and its invariants are the test suite.

## 0. Design principles (user-locked, 2026-07-27)

1. **Simple.** The model fits on an index card. Anything that doesn't earn its place is out.
2. **Only reliably computable inputs.** Every number in the scoring path is KTC price data, roster arithmetic, or a fact about a completed transaction. No estimated probabilities, no simulated opponent utilities, no invented constants. (KTC itself "is really a loose estimate" — the user's words — so no precision machinery gets built on top of it.)
3. **Rational roster management is assumed**, for every team including mine. Roster counts and pending cuts are housekeeping, not signals: "just because somebody is over the roster limit, that does not mean that they are a seller."
4. **Strategy**: wealth arbitrage between postures. Sell veteran players to teams that want to win now; buy players from / sell picks to teams that are rebuilding. Every trade pairing ends roster-neutral. Deadlines are execution logistics, not score inputs.

## 1. Data contract

| Input | Source | Rule |
|---|---|---|
| Player value `v(a)` | KTC `oneQBValues.value` (0–9999), joined via `data/ktc_sleeper_map.json` crosswalk | Canonical. Never superflex, never TEP variants, never production stats. Missing from KTC ⇒ `v = 0` + `unvalued` flag (display "—", never treat 0 as truth). |
| Pick value `v(p)` — ONE price (v7.5) | **Current year:** KTC's NUMBERED pick, regenerated by `core.scoring.ktc_picks` · **Future years:** the round's MID tranche (v7.6) | The price EVERY consumer reads — the §3 gate, favorability, the anti-fleece ratio, the return denominator, ΔF, the deep links, the league tab and the hedge board. **Current year (v7.4): the NUMBERED price.** KTC publishes one for the exact slot ("2026 Pick 3.02") and GENERATES those 48 records client-side, so they are in no payload we scrape; `ktc_picks` ports that generator, pinned 48/48 exact against a one-shot live capture. Two of the 48 are fractional because KTC's are — it derives 1.01/1.02 off the rookie ladder and passes the unrounded figure into its own adjustment, so we carry the fraction rather than round away from their number. (Pricing a current-year pick at the generic tranche is what made our gate read 9,415/10,069 against a page showing 9,421/10,198; §11.15 pins it as a negative.) **Future years (v7.6): FLAT MID, NEVER a forecast.** KTC publishes nothing finer than Early/Mid/Late, and the engine never estimates where inside the round a pick will land: every future pick prices at its round's Mid tranche, whoever owns it. The price vector is therefore SYMMETRIC between seats — my 2028 2nd and yours are the same number, and a same-year same-round swap is exactly ΔF 0. Two retired alternatives, both pinned as negatives in §11.15: the `rank_L` projection (through v7.4 — a forecast of the origin team's finish) and v7.5's directional pessimism (Early when I send / Late when I receive — no forecast, but seat-asymmetric; the user chose the neutral midpoint). Zero free parameters: Mid and the numbered price are literal KTC records. |
| Pick MY value `v_me(p)` (v7, degenerate since v7.5) | identical to `v(p)` | v7 carried a pessimistic second lens while `v` kept a market projection; v7.5 collapsed the two into one price, and v7.6 settled that price on flat Mid — so `p == mv == p_me` for every pick. The field and the ΔF plumbing survive unchanged (ΔF still reads `v_me`), and every emitter treats `v_me != v` as a tripwire to DISCLOSE, never a state to hide — if the lenses ever re-diverge, the cards and the board say so. |
| Rosters, picks, taxi, IR, FAAB | Sleeper API | Refreshed daily + on demand (unchanged collector). |
| Trade history | Sleeper transactions (both league seasons) | Sole source of posture classification (§4). |
| League fairness norms | Measured from this league's completed trades | §3 constants; re-derivable from the transaction log. |

## 2. The score — two objective coordinates, no blend (v4)

The user's ask, verbatim: "a deterministic way to value one of these spreads. I want to know 'Will this trade make my team better and by how much?' If possible, I want an objective calculation that works in all cases." v4 delivers exactly the objectivity that is mathematically available — and no more.

Every trade's effect on a side decomposes into two parameter-free numbers, both pure roster arithmetic on KTC face values:

```
ΔS = change in STARTER value: the max-Σv legal lineup (QB / 2 RB / 3 WR / TE / 2 FLEX)
     solved over active + taxi players at raw KTC v, after minus before
     (taxi startable — promote-anytime, §8; IR and empty slots = 0;
      for a pair, BOTH legs applied together — legs interact through the lineup)
ΔF = change in TOTAL FACE owned: Σ v(in) − Σ v(out), players and picks alike
     (one price per asset — §1 v7.6: players at KTC v, picks at the exact slot
      this year and the flat Mid tranche beyond. Additive across legs — the
      property every bound rests on — and conserved across a leg's parties)
```

Any single-number ledger is `ΔW(δ) = ΔS + δ·(ΔF − ΔS)` for some stored-value preference δ — and **δ is a time preference (win-now ↔ win-later), not a measurable fact**. It is bounded for every rational manager: free disposal ⇒ δ ≥ 0; a starter is a stored asset plus the free option of fielding him ⇒ δ ≤ 1. `ΔW(δ)` is linear in δ, so its value across ALL rational preferences ranges exactly between its endpoints: `ΔS` (at δ = 0) and `ΔF` (at δ = 1). Therefore:

- **Verdict (objective, works in all cases):** a spread is better for EVERY rational preference iff `ΔS ≥ 0 AND ΔF ≥ 0`, at least one strict. This is the user's strategy statement formalized — "increase my team's strength by … capturing edge on both legs." Face-only readmits the two-QB disaster; starter-only readmits bench-stripping; the two coordinates police each other. The board recommends ONLY objectively-good spreads.
- **Magnitude (objective):** the gain is the interval `[floor, ceiling]`. Since v8.2 the REPORTED interval also worst/best-cases every future pick inside KTC's PUBLISHED Early/Mid/Late band: `floor = min(ΔS, ΔF + down)` and `ceiling = max(ΔS, ΔF + up)`, where `(down ≤ 0 ≤ up)` is the **pick-band swing** — my sends landing Early and my gets Late for `down`, the mirror for `up`, netted by `(year, origin)` because one origin team's finish decides the slot of ALL its picks that year (same-group picks on opposite sides cancel exactly; each independent group is worst-cased over {Early, Mid, Late}, so a group that cannot hurt me contributes 0). Current-year picks have a known slot and no swing, so a pick-free trade's interval is exactly the old `[min(ΔS, ΔF), max(ΔS, ΔF)]`. The **floor is the guaranteed gain** — the worst case over every rational preference AND every in-band slot outcome — with one honesty caveat, always disclosed: KTC's Early/Late tranches are band AVERAGES, not hard bounds (a true 1.01 beats the Early tranche), so "worst" means worst within KTC's published band. A single point inside the interval would require choosing a δ or a slot forecast; neither is chosen, anywhere.
- **Ranking (objective, maximin, NEUTRAL — §11.17):** guaranteed floor descending; ceiling descending as tie-break; deterministic ids last — both keys computed at the flat-Mid convention, never from the band-extended interval. The swing is a REPORT: it never enters a selection, retention, or ordering key (a row's shown interval can therefore sit slightly out of the displayed order — disclosed on the board). No preference parameter exists in the system.
- **Preference trades are labeled, not scored.** A spread failing one coordinate is not "bad" — it is preference-dependent, and carries an objective breakeven `δ* = ΔS / (ΔS − ΔF)`: the QB case below is good exactly for δ > 0.5 (a rebuilder's trade). The CLI reports the breakeven for any scored proposal; the board never recommends these.
- **The two user QB cases (locked 2026-07-28) restate as verdicts:** 8,000 + 4,000 → 7,000 + 6,000 is (ΔS −1,000, ΔF +1,000) — NOT objectively good, breakeven δ = 0.5. A 5,000 → 6,000 backup upgrade is (ΔS 0, ΔF +1,000) — objectively good, gain 0 to +1,000 (floor 0 ranks it honestly low). A non-starter sold for a same-face pick is (0, ≈0) — the v3.4 reclassification exploit stays dead with no parameter needed. **v7 makes it deader:** when the pick is a FUTURE one I receive it at the cheap end of its round, so the same conversion reads (0, negative) and the trade is bad at every rational preference rather than merely worthless.
- **Raw v only.** The S-solve is the greedy per-slot fill (exact for raw Σv), deterministic ties by KTC playerID asc. None of the league-tab lineup machinery — q insurance weights, availability multipliers, replacement values — enters the trade path (§11.2).
- **Per-side, both coordinates, one price vector.** Each side's (ΔS, ΔF) is computed against its own roster. ΔS was never zero-sum — deployment differs by roster, which is why both sides of a good spread can genuinely gain. ΔF is (v7.5 restored it): one price per asset, so `ΔF(them) = −ΔF(me)` exactly on every leg (§11.1) — and since v7.6 the vector is SYMMETRIC between seats too (§11.1b).
- **What the flat-Mid convention costs the objectivity claim (v7.6, restating v7/v7.5).** `ΔW(δ)` above is the δ-family of ONE price vector applied to ONE inventory. An unknown-slot pick has no observable price, so SOME stand-in is unavoidable; the interval `[ΔS, ΔF]` is that family **evaluated at the round's midpoint** — a stated convention, not a measured fact. The defence is that the convention introduces **zero free parameters** (Mid and the numbered board are literal KTC records; nothing is fitted or forecast — v7.5 deleted the `rank_L` band, the one number that WAS a forecast) and is **symmetric** (it cannot manufacture ΔF on a pick swap in either direction — same-year same-round is exactly 0). What the PRICE does not provide is caution: v7.5's directional pessimism only ever erred against me, and the midpoint deliberately trades that guarantee for neutrality — the user's call, twice considered. **v8.2 restores the caution as a REPORT**: the interval above folds the band's worst/best case (the exact quantity v7.5 charged as a price) while the price, the gate, the verdict and every ranking stay flat Mid — the §11.17 firewall is what makes the caution safe to have back, because v7.5's measured failures (the gate flipping REJECT→PASS at 12.1% on §10.1; the counterparty-friendly frontier emptying 184→0) both ran through numbers the gate compares, and a report touches none of them. §0 principle 2 survives in the sense that matters: nothing here is fitted, simulated, or invented — Early and Late are literal KTC records, loaded since v7 and simply never read for future years until now.
- **Nothing else moves the score.** Not roster counts, not pending cuts, not deadlines, not probabilities, not the counterparty's internal valuation, and not KTC's consolidation adjustment (that prices trades in §3; it never values them). §11's regression invariants enforce each exclusion explicitly.
- A spread with `0 < floor < W_min = 150` carries a display-level noise note (inside KTC's own error bars), never hidden.

## 3. The fairness gate — "would they think it's fair?"

League-mates evaluate trades by checking KTC — the same site this system scrapes. The gate models exactly that check, with tolerances **measured from this league's ~33 completed trades**, not invented:

1. **Exact calculator band (v3.4).** Packages are compared exactly as KTC's own trade calculator compares them, via the reverse-engineered value-adjustment algorithm (`core.scoring.ktc_adjust`, a faithful port of keeptradecut.com's client-side logic, pinned by §11.11 to 13 live calculator trades captured 2026-07-27). Per side, assets sorted `v` desc, each contributes `(0.05·(v/C)^1.3 + 0.05·(v/(1.05·R))^6 + 0.1)·v` — `C` = top overall KTC value + 80, `R` = largest asset in the whole trade, both computed from the snapshot; a side's 2nd/3rd/4th+ assets below `R/2` take ×0.85/0.70/0.60 haircuts. The per-side sums are mapped back to value space with KTC's exact iterative inverse and the concentrated side is topped up so the final adjusted gap equals KTC's deserved gap. Require `|adjTotal(give) − adjTotal(get)| ≤ max(500, 0.20 · max adjusted side)` — band tolerances stay the league-measured ones. The fitted consolidation coefficients `c = (1.00, 0.90, 0.80)` are retired: they approximated this algorithm, and the adjustment provably does NOT cancel in roster-neutral pairs (≈ +3,377 for {8000, 2000} over {5000, 5000} at equal raw totals — "Value adjustments dont cancel out").
2. **Anti-fleece cap.** Raw `Σv` ratio ≤ **1.35**. Never exempted: fleeces don't clear, and reputation is an asset in an 11-opponent repeated game.
3. **Legality.** Both post-trade rosters legal: positional minima, roster caps with taxi routing per the taxi mechanics (§8), IR rules, trade deadline week 11.

**Enumeration policy (v3.3 — no pruning before pairing):** the engine keeps EVERY in-band, fleece-clean, legality-clean package variant (deduped by asset multiset) — v3.1's "one fairest get per give" pruning starved the pair matcher and is retired ("our pair and prune logic is severely broken"). Selectivity moves to the user's **target return range** (§5; v3.3.1 — min/max over the presets). The band remains a hard per-leg cap; the band-edge ceiling and the +8% anchor-ask convention remain card annotations. `W_min` retires as a gate (kept only as a display-level noise note in the CLI).

## 4. Targeting & market constraints (v5 — one vocabulary, three sources)

**Posture** is classified from **observed trades only**, over the trailing 12 months:

```
For each team: count completed trades where net flow = players in / picks out ("bought"),
and the reverse ("sold"). BUYER if bought ≥ sold+1 and bought ≥ 2 · SELLER mirrored ·
else NEUTRAL. Inactive (< 2 trades) ⇒ NEUTRAL.
```

Each label is displayed **with its evidence** and is user-overridable per team (`posture-overrides`).

**v5: every targeting fact is a CONSTRAINT in one machine-executable vocabulary**, applied by the spread finder (§4a):

```
constraint = { who: <username> | "me" | "*",
               side: "receives" | "sends",
               what: {class: "pick"|"player"} | {pos: "QB"|"RB"|"WR"|"TE"} | {asset: <name|key>},
               with: <username> | null,          # optional: only vs this counterparty
               mode: "require" | "exclude" | "prefer" }
```

- `require` — every leg involving `who` must match (hard). `exclude` — no leg may match (hard). `prefer` — matching pairs are tagged ★ and can be toggled to only-preferred; never reorders slider math (soft).
- Semantics are per-leg: `who receives what` constrains the package flowing TO `who` on any leg they are party to. `who = "*"` means every counterparty; `me` is my own side.

**Three sources, strict precedence (later wins per-team):**
1. **Posture defaults** (auto-compiled): BUYER → `{who: X, side: "receives", what: {class: "player"}, mode: "require"}`; SELLER → same with `class: "pick"`; NEUTRAL → no constraint. (This subsumes v3.3's posture-as-hard-constraint — identical behavior when nothing else is said.)
2. **Market intel** (`market-intel`, the skill's protocol): WANT → require on their receive side; DONT_WANT → exclude on their receive side; OFFERED(asset) → prefer on MY receive side vs that team; REJECTED → exclude of that exact shape; NOTE → no compilation. Active intel auto-applies; each item is individually revocable.
3. **Query constraints** (ad-hoc, per §4a call) — override both for the teams/assets they name.

Compilation of the user's own examples: "ronak wants draft picks" → `{ronakpatel32, receives, class:pick, require}` · "joey wants a running back" → `{joeydavis299, receives, pos:RB, require}` · "nobody wants stefon diggs" → `{*, receives, asset:Stefon Diggs, exclude}` (equivalently: Diggs is protected — never leaves my roster) · "josh b wants joe burrow" → `{josbaski, receives, asset:Joe Burrow, require}` · "colin is willing to trade away treveon henderson" → `{me, receives, asset:TreVeyon Henderson, with: cmgaither43, prefer}`.

- **Aim at visible holes** (carried): per-position starting-lineup sums annotate *which* players to offer — information, never a score term.
- **Two offers to the same team are noted** (carried) — appetite is psychology, which stays with the user.

## 4a. The spread finder (v5, amended v6) — deterministic constrained search with three sliders

The interactive query engine behind the trade-negotiator skill (and the CLI `find` command). The legal spread space runs to millions; the finder makes constrained lookups and constraint-ADDING cheap:

- **Leg cache.** Per snapshot, the per-team package lists (give-lists × asset metadata: keys, classes, positions, Σv, count signatures) and my starter index are computed once and disk-cached under `.cache/` keyed by (snapshot fingerprint, params hash). Cold build is a one-time cost; every subsequent query — including re-queries with added constraints — loads warm and re-enumerates only the CONSTRAINED subsets. Warm and cold results are byte-identical (§11.12).
- **Query pipeline (deterministic):** compile constraints (§4) → filter each side's candidate packages BEFORE gate work → enumerate surviving legs per counterparty (exact §3 gate: band, fleece, legality) → cross buy×sell (asset-disjoint, distinct counterparties, count-neutral 0/0) → exact combined coordinates per §2 (incremental starter evaluator) → slider filter/sort → top `finder_top` (20) with honest counts. Constrained crossings small enough to finish exhaustively report EXACT counts; anything hitting `finder_cross_budget` saturates to verified floors (§5 doctrine). Favor bounds push down to LEG level before the crossing (v5.0.1: pair `min ≥ m` ⟺ both legs ≥ m, pair `max ≤ M` ⟺ both legs ≤ M — favor is a per-leg quantity, so the decomposition is exact), and a favor-banded query is exhaustive whenever the filtered crossing fits the budget. Numeric-δ walks order the crossing by the per-leg δ-score heuristic `leg_ΔW(δ) = iso_ΔS + δ·(ΔF_leg − iso_ΔS)` rather than the isolation floor; combined ΔS stays non-additive, so that order remains a disclosed heuristic — truncated δ-view counts are floors, never certificates.

**Sound crossing bound (v5.1) — the robust/floor objective is now COMPLETE, not heuristic.** Through v5.0.1 the floor-objective walks ordered and pruned on each leg's ISOLATION floor, `min(ΔS_leg, ΔF_leg) − r·Σsent_leg`, implicitly treating the pair floor as additive. It is not — ΔS is jointly determined by both legs through the lineup — so pairs whose legs are individually weak but complementary were pruned unseen. Measured on a simulated roster state: a robust favor-banded query returned **0 matches** where an exhaustive crossing of the same state held **4,157,460 verdict-true pairs, 88,911 of them clearing the query's own 1% bar** (best 3.00% return, floor +439; its buy leg shipped a starter and scored terribly alone). v5.1 replaces the key with

```
u(leg) = ΔF(leg) − r · Σsent(leg)          [prune when u_buy + u_sell < 0]
```

which is SOUND: `ΔF` is exactly additive across legs (§11.1 face conservation) and `floor = min(ΔS, ΔF) ≤ ΔF`, so `u_buy + u_sell < 0 ⟹ ΔF_pair < r·sent_pair ⟹ floor_pair < r·sent_pair ⟹ return < r`. The bound can never discard a qualifying pair. The walks test it in SPLIT form (`(r·sent_b − ΔF_b) + (r·sent_s − ΔF_s) ≤ 0`), which disagrees with the pair form on exact float ties — reachable, since ΔF and Σsent are integral and `r` is often round — so every walk carries a one-sided tolerance `XTOL = 1e-6` KTC points that keeps the boundary; the complementary below-walks apply the same tolerance with the same sign, so the two still partition the crossing space exactly. Consequences: a floor-objective walk that runs to completion is **EXACT** — its counts are provable and "no spread matched" is a real answer, not "none where I looked"; only budget-truncated walks remain verified floors. **Scope (v5.1, honest reading):** exact means exact over the POOLED legs. §5's per-signature variant pre-ranking (`variants_per_signature` gets by isolation floor out of the first `variant_scan_cap` gate-passers) is the same additivity fallacy applied to leg SELECTION, and the sound bound does not repair it — measured on the pinned scoped query, relaxing the caps to 6/12 exposes 829,914 further gate-clean pairs the default pool cannot represent at all. So "none exists" always means "none among the pooled legs", never a claim about the whole legal package space; every consumer sentence says so. Re-keying or widening the pool is a separate, unpriced change. Cost: `ΔF` typically runs 2–5× the floor, so the bound gives up later and visits more crossings per qualifying pair (measure per §11.10). The δ-view objective `ΔW(δ) = (1−δ)·ΔS + δ·ΔF` admits an analogous sound bound via `ΔS ≤ Σ face received` (also additive), but it is far looser; δ-views keep the disclosed heuristic until the robust runtime cost is known.

**v6 — a tight sound bound on ΔS, and the crossing family that was never searched.** Two amendments to the above.

*(a) The Δplayers == 0 family.* Every walk opened with `if sig[0] <= 0: continue`. Since a spread's legs have complementary signatures summing to (0,0), that rejects BOTH sides of the Δplayers == 0 family — 26.8% of live legs, of which 41,722 sit in (0,±1)/(0,±2) and are genuine hedges (swap players evenly with two teams, net a pick from one to the other). `canonical_sig(sig) = sig[0] > 0 or (sig[0] == 0 and sig[1] > 0)` replaces it everywhere. **(0,0) is deliberately excluded**: such a leg is roster-neutral alone, so pairing two is two unrelated even swaps rather than a hedge, and ~1.3e9 such pairings would swamp the board.

*(b) `ΔS_pair ≤ A_buy + A_sell`.* Each leg stores `A`, the add-only starter gain of its RECEIVED package — a function of the get package alone, since `pos_columns` drops picks, so it is solved once per distinct package rather than per leg. Starter sum is the max-weight basis of a transversal matroid over the startable pool, hence monotone submodular, so `L(R − out + in) ≤ L(R + in)` (removals only lower it) and `L(R + in_b + in_s) − L(R) ≤ [L(R+in_b) − L(R)] + [L(R+in_s) − L(R)]`. Audited 0 violations over 24.4M crossings with NO tolerance. This supersedes `ΔS ≤ Σ face received` as the δ-view bound and makes the pair floor fully separable: `floor ≤ min(A_b + A_s, ΔF_b + ΔF_s)`. Any blended key built from the two halves must use INTEGER coefficients — `3·ΔF + 2·A` against `5·θ`, never `0.6·ΔF + 0.4·A`, which has a real one-ULP counterexample (ΔS = ΔF = A_sum = 748, blended bound 747.9999999999999).

*(c) `with_team` scoping.* The scope test ran inside the innermost loop after the visit was already charged, so a per-counterparty query walked the league-wide space and discarded most of it. Legs in a pair always have distinct counterparties, so "buy leg is theirs" and "sell leg is theirs" are mutually exclusive and jointly cover every in-scope pair — the scope is an exact PARTITION, pushed onto the sell list per buy leg so both orderings (and every truncation) are unchanged. The measured fair-window per-counterparty queries now complete exhaustively at 3.4–5.6M crossings instead of saturating 16M.
- **Slider 1 — δ (starter-friendly ↔ team-friendly):** view score `ΔW(δ) = ΔS + δ·(ΔF − ΔS)` and `return(δ) = ΔW(δ) ÷ Σ face sent`, δ presets 0 / 0.25 / 0.5 / 0.75 / 1 plus **robust** (default: v4 floor/verdict — good at every δ). Moving the slider is a labeled preference VIEW; §2's objective verdict is unchanged and always shown. Shape predicates are constraints, not sliders: `starter>team` ⟺ ΔS > ΔF, etc.
- **Slider 2 — total return:** threshold/sort on `return(δ)` at the selected δ (robust mode uses the guaranteed floor return). Presets as §5.
- **Slider 3 — counterparty favorability (KTC's own units):** per leg, `favor f = 100 · (adjTotal(they receive) − adjTotal(they give)) ÷ (adjTotal sum)` from the SAME §3 exact-adjusted totals the gate reads — the signed version of KTC's calculator equality metric, so `|f| ≤ 5` is literally "their calculator says FAIR at default variance", `f > 0` skews to them. Pair favorability = `min(f_buy, f_sell)` (the least-happy counterparty). Slider = floor on pair favorability, presets −10 / −5 / 0 / +2.5 / +5 / none; an optional ceiling stops giving edge away. The §3 band stays the hard outer bound — favorability selects WITHIN it.
- **Structural filters:** `--legs A+B` (one leg with A, the other with B), `--with X` (X on some leg), must-move / protected assets (sugar over require/exclude).
- **Efficiency contract:** adding a constraint NARROWS candidate sets — re-query cost is monotone non-increasing warm; slider changes never re-enumerate (coordinates and favor are stored per found spread; re-filter/re-sort is O(results)).

## 5. Pairings, the book, and execution

**Where this runs (v7.1).** The pair board is **not** part of the nightly
`compute_all` and no `trade-recs` document is stored. The dashboard carries
Waivers and League only; spread-finding is the CLI (`scripts/score_trade.py
pairs` / `find`) and the trade-negotiator skill, which call
`trades.trade_board` and the §4a finder directly and always computed their own
board live rather than reading a stored one. Everything below still describes
exactly what those produce — the dials, the stratified storage, the honest
counters — it just runs on demand instead of overnight. Motivation was cost:
the pair pool is by far the most expensive pass in the run (it puts the exact
KTC gate and a starter re-solve inside the whole enumeration), and once the tab
was gone nothing read the result. The §5/§11 invariants are unchanged and stay
pinned against `trade_board` directly.

- **Target return (v4 numerator):** define `return(pair) = guaranteed floor ÷ Σ v(assets I send across both legs)` — the §2 worst-case gain, `min(ΔS, ΔF)` with ΔS combined across both legs, on face-value inventory deployed. **The denominator is face `v` of what I send** — the numerator is what the trade is worth to me, the denominator what I deployed. (v7 kept the denominator on a separate market lens; since v7.5 there is one price, so the ratio is single-lens — sent picks enter at the same flat Mid every other consumer reads, v7.6.) `L_SENT` in the leg tuple is that denominator. Legs are pre-ranked inside the pool by isolation floor as a disclosed heuristic; every STORED pair's coordinates are exact combined. Pure stored-value conversions have floor ≈ 0 and fall below the 1% stored universe automatically. **v5 — the board's dials are the finder's sliders (§4a), over stored inventory.** Three client-side controls: **δ selector** (robust default + presets 0/0.25/0.5/0.75/1 — re-scores stored pairs as `return(δ)` instantly from stored coordinates, labeled as a preference view), **min total return** (on `return(δ)`; robust mode = guaranteed-floor return; presets 1 / 2.5 / 5 / 10 / 20%, default 5), and **counterparty favorability floor** (replaces v3.4.1's raw-skim leg cap — the raw skim provably diverges from what the counterparty's calculator shows by up to 14 pts; presets −10 / −5 / 0 / +2.5 / +5 / no floor, applied to `min(f_buy, f_sell)` in §4a's exact KTC units). Sort: `return(δ)` desc, ceiling desc tie-break. Storage is **stratified by favorability bucket**: pairs bucket by `min(f_buy, f_sell)` over (−∞,−10), [−10,−5), [−5,0), [0,+5), [+5,∞); per bucket the nightly run keeps the UNION of top `pairs_per_band` (100) by robust floor-return, by ΔS, and by ΔF (deduped) — so both δ-slider extremes have inventory — plus per-bucket honest counts and a bucket × robust-return-band grid (`bands`: {lo, hi, stored, count, saturated, by_total}; **v5.1: a count is EXACT when its floor-objective walk ran to completion under the sound bound (§4a), and a verified floor only when the crossing budget truncated it** — `saturated` distinguishes the two, and the board discloses which). Each stored pair carries `coords`, `floor`/`ceiling`, `favor {buy, sell, min}`, and Σ sent, so every dial move is O(stored). Stored universe: robust floor-return ≥ 1%. Deep or constrained queries beyond stored inventory belong to the finder (§4a), not the board.
- **Posture is a hard constraint in the engine (v3.3):** the sell side of a pair must go to a **BUYER** (they receive players), the buy side must come from a **SELLER** (they receive picks); **NEUTRAL** counterparties accept either shape. Overrides (`posture-overrides`) apply before constraint evaluation. In the trade-negotiator skill the same constraints are qualitative — user intel can promote/demote a team's effective posture or exclude counterparties entirely.
- **The recommendation unit is the PAIR, fully count-neutral (v3.2, strict):** a **buy side** (I receive a player, paying picks — aimed at SELLER-posture counterparties) matched with a **sell side** (I send player(s) for picks — aimed at BUYERs), sharing no assets, and netting — for my side — **exactly `Δ(player count) = 0` AND `Δ(pick count) = 0`** ("completely neutral: we should end with the same amount of picks and same amount of players as before"). Players count wherever they land (active or taxi); picks count as picks regardless of year. No executed plan may inflate either inventory: the bundling wedge is harvested as value-per-slot upgrades at constant counts, never as asset-count accumulation. **Unpaired legs of any direction are building blocks, not executable recommendations** — sells and neutrals list in a labeled secondary section with their count deltas; buys with no exit sit on the watch list with the blocker named.
- **Ranking (v4 maximin; v3.4.1 filter):** pairs sort by TOTAL `return_pct` (NEUTRAL flat-Mid floor return — §11.17) descending within the user's filter (total floor + per-leg cap), neutral ceiling (max of the coords, recovered from `coords` since the doc's shown `ceiling` folds the v8.2 pick-band swing) desc as tie-break; every stored pair passes the §2 objective verdict (ΔS ≥ 0 AND ΔF ≥ 0) — posture fit is no longer a rank key because it is a hard pool constraint (§5 posture bullet); every leg individually passes §3. Pair cards carry an `overlaps` count (shared-asset conflicts against other stored pairs); unpaired-leg data keeps exact `exclusive_with`.
- **Execution protocol (rules, not math):** agreement-first — negotiate the buy to a verbal yes, execute the sell-leg, execute the buy minutes later (Sleeper trades process instantly, `trade_review_days = 0`). With open roster spots the buy may execute first; at the cap, sell first. Don't publicly fire-sale before making buy-side asks. After any executed trade, the whole board recomputes from fresh rosters.

## 6. Waiver tab

Mechanics carried from v1 (they were already empirical):

- **Claim score** `= v(add) − v(drop)`, drop = my lowest-`v` active (rational housekeeping). Positive-score claims list with the drop named.
- **Bids:** offseason $0 default / $1 when contested (waivers process daily, budget $50 until ~Aug 12 then $200); in-season ladder exactly as v1 §6.4 with its two 2025 backtests (Waller $63 vs actual $60; Kyler $117 vs actual $115) — the in-season formula's `ΔL` allocates FAAB dollars to weekly lineup needs, a separate currency from trade wealth, explicitly outside the no-lineup-term rule which governs trades.
- **Drop list:** actives ascending by `v` — informational housekeeping.
- **Taxi fill:** pre-lock, surplus free taxi slots (slots beyond incoming-pick needs — pure arithmetic) list the best stashable 1st/2nd-year FAs; an empty locked slot is worth zero.

## 7. League tab

- **Strength map** (carried): per-team per-position starting-lineup sums and ranks; strictly separated **future assets** (picks at the §1 one price + taxi values). v7.6: flat Mid is owner-independent, so the F column is a neutral survey of all 12 inventories again (v7.5's directional pricing briefly made it my seat's read, flattering exactly one row). Informational.
- **Market map** (new): each team's posture label with its evidence trades, positional holes, pick inventory, FAAB. This is the targeting console.

## 8. Roster mechanics (factual league rules, carried)

Taxi errata 9–12 semantics (1st/2nd-year only, week-4 lock, promote-anytime, slots not refillable post-lock, surplus-slot routing for incoming eligible players), IR eligibility (game-status OUT, 2 slots), positional minima, week-11 trade deadline. These affect **legality and sequencing only** — never the score.

## 9. Parameters

| Parameter | Value | Origin |
|---|---|---|
| `W_min` | 150 | Noise floor inside KTC error bars |
| ~~Stored discount `δ`~~ | RETIRED (v4) | The score has zero parameters: two objective coordinates + maximin. δ survives only as the derived, per-trade breakeven the CLI reports on preference trades |
| Fairness band | 20% relative, 500 floor | Measured from league trades |
| KTC adjustment | exact port, zero free parameters | Reverse-engineered from keeptradecut.com `site.min.js`; 13/13 live calculator trades integer-exact (2026-07-27). Supersedes fitted consolidation `c` |
| Fleece cap | 1.35 | Measured (max observed cleared ratio) |
| Anchor ask | +8% | Observed negotiation convention; display only |
| Posture window | 12 months, min 2 trades | Definitional |
| Waiver bid ladder | v1 §6.4 values | Backtested on 2025 bids |
| δ presets (v5 slider) | 0 / 0.25 / 0.5 / 0.75 / 1 + robust default | View presets only — never score parameters (§2/§4a) |
| Favorability presets (v5) | −10 / −5 / 0 / +2.5 / +5 / none | KTC's own variance units; ±5 = their calculator's FAIR window |
| `finder_top` | 20 | Query result size (finder) |
| `pair_collect_budget` | 4,000,000 (v6; was 400,000) | Stored-pair collection visits. Raised WITH `canonical_sig`, not independently: restoring the Δplayers == 0 family grew the crossing space, and a fixed budget over a larger space stores FEWER pairs. Measured live: 400k → 619 stored / 19 both-legs-fair; 1.6M → 1,101 / 478; 4M → 1,133 / 528, at 61.0 s and 236 MB against the collector's 900 s / 2048 MB. A mitigation, not a cure — the budget is a walk cutoff and still certifies nothing |
| `pool_favor_band` | `None` (v6) | When set to β, `build_pair_pool` DRAINS each bracket (no `variant_scan_cap`, no `variants_per_signature`) and screens on that band's own bound, giving the complete |favor| ≤ β inventory. Default `None` keeps v5's sampled scan and byte-identical output, because the complete β = 5 pool measures 11,911,255 legs / **3,904 MB** as Python tuples holding `Package` objects. Columnar legs are the prerequisite for turning this on by default |
| `screen_slack` | 1.15 (v6) | Multiplier on the raw-adjustment-gap screen threshold. Covers `reverse_adjust`'s 2.5% tolerance, which the port does not always honour (it is non-monotone). **Calibration, not proof:** the minimum factor admitting every fair leg on the live snapshot measures 1.0308, so this carries ~11.6% headroom |
| ~~`variant_scan_cap` / `variants_per_signature`~~ | 3 / 2, wide-band path only (v6) | The v3.4/v5 SAMPLING. Retained only for `pool_favor_band = None`; inside a band the bracket is drained. It retained 0.449% of the fair inventory and, because the bracket was scanned Σv-DESCENDING, skewed the survivors hard toward the me-favorable end (pool favor median −9.40 vs a true population median +0.10) — worst at exactly the counterparty-friendly trades a spread needs |
| `finder_cross_budget` | 20,000,000 (v7.5; 16M at v5.1, 2M before) | Crossing budget per query; saturation → verified floors. Raised at v5.1 with the sound bound: ΔF gives up later than the retired isolation key, so a TRUNCATED robust walk keeps measurably worse crossings for the floor objective — the measured market query returned best 13.18% and none of the exhaustive top-20 at 2M, and crossed COMPLETELY at 10.3M (best 16.25%, `exact=True`, 34.5s). Raised again at v7.5, whose pessimistic pricing pushed the largest per-counterparty dashboard crossing to 16,110,117; v7.6's flat Mid shrank that max back to 10,358,086, and the 20M ceiling stays — a budget only bites on queries that would saturate anyway, and the headroom keeps the per-counterparty board exhaustive across re-pricings (~576k crossings/s; unconstrained queries, space 4.6e9, stay honest floors either way). The finder is the interactive path — never the collector Lambda — so §11.10's board budget is untouched. |

Nothing else. Posture labels are user-overridable; every parameter above is re-derivable from data; the SCORE still has zero parameters — sliders are views.

## 10. Worked examples (pinned to the committed `data/` fixtures, 2026-07-26 KTC values)

1. **Sell-leg, preference trade / gate no (v4 coordinates):** Mike Evans (4,125) + Courtland Sutton (3,674) → jaketoppen for vishan's 2027 1st + jaketoppen's 2028 4th. **v7.6:** both picks price at their round's flat Mid for EVERY consumer — **6,118** and **1,759** (the retired rank_L lens read vishan's 1st Early 7,398; v7.5's pessimism read it Late 5,562; the midpoint splits the difference). My side: **(ΔS −64, ΔF +78)** — a PREFERENCE trade, good only for δ beyond **δ\* ≈ 0.4507**, floor −64. Not the +1,358 gift the Early projection once claimed, and not the certified −786 loss the Late pricing read either: at the midpoint, buying future picks at a market-fair price is roughly a wash whose sign is a stored-value preference. Their side: **(ΔS +1,216, ΔF −78)**, breakeven δ\* ≈ 0.9397 — one symmetric price vector, so the two ΔFs negate exactly. Gate: 7,799 against 10,262, a **2,463 gap = 24.0%** against a 20% band ⇒ **REJECTED** (favor −13.6; ratio 1.01, clean) — between v7.4's 36.8% (Early projection) and v7.5's 12.1% PASS (Late), because the concentrated 6,118 still earns a top-up two mid WRs do not. **v8.2:** the reported floor folds the pick-band swing — receiving vishan's 1st risks Late (−556) and jaketoppen's 4th (−308), so `down = −864` and **floor = min(−64, 78 − 864) = −786**: numerically the exact loss v7.5 once charged, demoted to a disclosure beside unchanged coordinates, breakevens and gate.
2. **Buy-leg, face for face (v4 verdict):** buy Jauan Jennings (3,001) from millj for my 2028 3rd. **v7.6:** the pick prices at the flat Mid tranche everywhere — **2,468**, numerically the pre-v7 market band again since two years out was always flat Mid (v7.5 briefly charged Early 2,618 because I was the sender). My side: **(ΔS 0, ΔF +533)** — Jennings does not crack my max-Σv lineup, so the whole move is in the face coordinate: **objectively GOOD** (no rational preference loses), gain **0 to +663** with a guaranteed floor of **0** (v8.2: my own 2028 3rd out risks Early −150, but 533 − 150 = 383 keeps ΔS = 0 the binding floor; the ceiling folds the pick landing Late, +130) — exactly where maximin ranks it: honestly low, never as a headline. millj's side is **(0, −533)**: one symmetric price vector, ΔF negates exactly. Gate: 2,468 against 3,898, a **1,430 gap = 36.7%** against a 780 band ⇒ **REJECTED** (ratio 1.22, clean). Shape is still right: picks → SELLER-classified counterparty.
3. **Gate rejection:** Cam Ward (4,426) for Shedeur Sanders (2,368): ratio 1.87 > 1.35 — never surfaced; fleeces don't clear.
4. **Objectively good with the gain pinned (the arbitrage, §2):** my Javonte Williams (5,460) ↔ ronakpatel32's Zay Flowers (5,651), no picks either way. My side: **(ΔS +191, ΔF +191)** — floor = ceiling, so the verdict is objectively good with the gain **exactly +191 at every rational preference**: the entire face gain lands in the lineup. Their side: **(ΔS +96, ΔF −191)** — a preference trade with breakeven **δ\* ≈ 0.3345**: a rational win-now manager (δ < 0.33) takes it, which is why the trade can close. ΔF is zero-sum (+191/−191, §11.1); ΔS is not (+191/+96) — deployment differs by roster, and that difference IS the arbitrage. **v7 and v7.5 leave this example untouched, and that is the point of pinning it**: no pick is involved, so no re-pricing can ever reach it. It is the invariant-1 witness. Gate PASS. It is the fixture's only gate-clean 1-for-1 where my verdict is good with a three-digit guaranteed floor, which is exactly why §11.1 pins it.

## 11. Implementation invariants (the test suite)

1. **Face conservation:** on every leg, Σ face `v` leaving one side equals Σ face `v` arriving at the other, exactly — every asset carries the same `v` on both rosters. This is the half the gate, favorability and the anti-fleece ratio rest on. v7.5 restored the conservation picture v7 had split (one price vector, so `ΔF(them) = −ΔF(me)` exactly on every leg) and v7.6 re-priced what `v` holds for a future pick to the flat Mid tranche.
1b. **The one price vector is SYMMETRIC (v7.6).** Flat Mid is owner-independent: my 2028 2nd and yours are the same number, a same-year same-round swap is exactly ΔF 0, and neither seat's printed coordinates embed a direction premium. (History: v7 priced caution on a second lens over a projected market band; v7.5 killed the projection but kept the caution as a seat-asymmetric vector — dear out, cheap in; v7.6 trades that caution for neutrality, the user's explicit call.) What every search bound actually needs is **additivity across legs on one side**, which survives untouched: `v` is a static per-asset scalar frozen at snapshot pricing time, no asset is ever priced two ways inside one pool, `ΔF_pair = ΔF_buy + ΔF_sell` is exact, and the §4a sound bound closes unchanged.
2. **Independence regressions:** perturb every league-tab lineup parameter (`q_*`, `u_*`, replacement) and every roster count — no recommended pair's coordinates change. An uninvolved bench player moves neither `ΔS` nor `ΔF` of any trade. Grep-level: the trade path imports only the raw starter-sum solve, never the q/u/replacement machinery.
3. Fairness gate: a generated recommendation never violates the exact-adjusted band, the fleece cap, or legality; the §10.3 fleece shape is a pinned must-never-emit.
4. Posture labels reproduce deterministically from the transactions fixture; evidence lists match; override wins.
5. Recommended pairs net exactly zero players AND zero picks for my side (v3.2); a player-neutral but pick-inflating pair is a pinned must-never-emit; `exclusive_with` fires on shared assets.
6. Waiver backtests (Waller/Kyler) keep passing verbatim.
7. Zero-value (`unvalued`) players never contribute to `ΔW` silently — cards flag them.
8. Taxi/IR/deadline legality rules (§8) enforced on both rosters of every recommendation; taxi players are startable in `S` (promote-anytime) and otherwise count only in face, IR players in neither.
8b. **v4 coordinate pins:** (a) the two §2 QB cases produce (−1,000, +1,000) → NOT objectively good with breakeven δ* = 0.5 exactly, and (0, +1,000) → good with floor 0; (b) a non-starter sold for a same-face pick has floor ≈ 0 and never enters the stored board (the reclassification exploit stays dead, parameter-free); (c) interval endpoints are the δ-extremes: `ΔW(0) = ΔS` and `ΔW(1) = ΔF` for arbitrary fixture trades — and `ΔF` is the v7 lens throughout, so `total_face(after) − total_face(before)` is recomputed in the SAME lens (`v_me` is frozen at pricing time and `apply_tx` carries it across, which is what keeps this an identity); (d) every stored pair satisfies `ΔS ≥ 0 AND ΔF ≥ 0` (one strict) — a verdict-violating pair is a must-never-emit; (e) stored order is maximin on the NEUTRAL flat-Mid figures: floor desc, ceiling desc, id — since v8.2 the doc's SHOWN `floor`/`ceiling` fold the pick-band swing (§11.17), so order checks recover the neutral keys from `coords`, never from the shown interval.
9. Determinism: identical snapshot ⇒ identical board, byte-for-byte.
10. Full nightly recompute within the v1 Lambda budget — the raw starter-sum solve enters the trade path (v3.4) via an incremental exact evaluator (asserted equal to a full re-solve over the fixture rosters and seeded random deltas), and the exact KTC gate runs behind a necessary-condition screen; the budgeted walks still saturate honestly, and under v3.4 every band count was a verified floor (§5) — **v5.1 supersedes that half**: a count is exact when its walk completes under the sound bound and a verified floor only when the budget truncates it (`saturated` says which; on the real nightly board it is still True everywhere, because ΔF's tail puts the affordable sound cutoff near 32% while storage starts at 1%).
13. **v5.1 soundness pins:** (a) the crossing bound never discards a qualifying pair — on a scoped fixture query the finder's matched set EQUALS an in-test brute-force crossing, element for element, and reports `exact=True`; (b) `floor ≤ ΔF` and `ΔF` additivity are asserted directly over fixture pairs (the two facts the bound rests on); (c) a constructed pair whose legs are individually below the return bar but whose COMBINED floor clears it is found by the finder (the regression that motivated the fix — pre-v5.1 it was pruned); (d) robust/board results may change vs v5.0.1 by ADDITION of previously-unreachable pairs — no pair that v5.0.1 returned may vanish for reasons other than being displaced from a top-N cut; (e) **boundary and phase integrity** — a crossing sitting EXACTLY on the bar (`ΔF_pair == r·sent_pair`) is kept by the top walk and excluded by the below-walk (the `XTOL` tie class, else the "never discards a qualifying pair" claim fails by one ULP), and the board's coverage phase hands a region back to the sound phase ONLY when that phase actually ran — the sound phase runs only where the crossing estimate proves it will complete (the estimate upper-bounds the walk's visits), because a phase that never enumerated a region must not own it: dropping an unwalked region loses storable pairs (measured on a starved walk: 487,795 crossings handed back, 49,709 storable), and the reverse would tally the same pair twice.
11. **KTC-adjust port regression:** the 13 captured calculator trades (2026-07-27 values, pinned as fixtures) reproduce integer-exact through `core.scoring.ktc_adjust`; a zero raw-adjustment gap short-circuits to S = 0.
13f. **v6 sound ΔS bound:** (a) `ΔS_pair ≤ A_buy + A_sell` over fixture crossings with NO tolerance — every input is integral, so a sound bound holds exactly and slack is allowed while a violation is not; (b) the same fixtures must still contain pairs whose joint ΔS EXCEEDS the isolation-ΔS sum, else the pin cannot distinguish the sound bound from the unsound one it replaces; (c) `A` is a function of the GET package alone — two legs receiving the same package have the same `A`, and it equals `index.delta(EMPTY4, in4)` — which is what licenses the per-package cache in `build_pair_pool`; (d) `canonical_sig` owns every crossing family exactly once, `(0,0)` is excluded by design, and `leg_type` follows the family rather than the player count.
16. **v7.4 numbered-pick pins:** (a) `ktc_picks` reproduces all 48 of KTC's generated numbered picks EXACTLY, in both the 1QB and superflex columns, from the 500 inputs of a same-instant capture — including the two non-integer prices the calculator derives off the rookie ladder, which are the tell that the special-cased top-of-round branch is ported rather than approximated; (b) the id/name encoding round-trips against KTC's own records (`parseInt(year+round+slot)`, slot unpadded in the id and zero-padded in the name) and has ONE implementation, shared with the deep-link builder; (c) the generator is order-insensitive, matching the JS's own two internal sorts — our snapshot's natural order is not value-descending and feeding it in raw moves 44 of 48 prices; (d) it REFUSES rather than degrades on every input that would otherwise yield a full, monotone, plausible, wrong board — a rookie ladder shorter than the pick count, a 13th same-year tranche, a year KTC does not number, an unported `LEAGUEYEARPHASE`; (e) every current-year pick satisfies `p == mv == p_me` and NONE equals its generic tranche — the negative is pinned, because reverting to the tranche is the silent failure that made the gate disagree with the counterparty's screen.

15. **v7.5/v7.6 one-price pins:** (a) every pick satisfies `p == mv == p_me` — the current year at KTC's numbered slot (v7.4), future years at the flat Mid tranche (v7.6) — and NO consumer sees any other number: `_pick_detail`, the trade cards and the board emit a second lens ONLY when `p_me != mv`, i.e. never, and that emitter survives as a pinned tripwire; (b) no forecast and no direction — all 144 fixture picks price by the two rules alone, `rank_L` prices NOTHING (vishan's 2027 1st, the projected 1.01, books Mid 6,118 — not the Early 7,398 the retired projection said — and DrewR87's books the same 6,118, not Late 5,562; `band_of_rank_l` no longer exists), and the vector is symmetric (millj's three 2027 R4s and my own all resolve to the same tranche id); (c) conservation is restored — `ΔF(me) == −ΔF(them)` exactly on every leg, pick-bearing or not; (d) `ΔF == total_face(after, lens="me") − total_face(before, lens="me")` holds on arbitrary fixture trades (`apply_tx` carries the minted price across), and the two `total_face` lenses agree; (e) additivity is untouched — `ΔF_pair == ΔF_buy + ΔF_sell` exactly, which is what the §4a bounds actually use; (f) the gate's 13 live-calculator captures (§11.11) are UNTOUCHED — the port is pinned on raw value lists, independent of what we feed it.

14. **v6 `ktc_fast` identity:** the memoized gate agrees with the pinned port BIT FOR BIT, compared by `repr()` and never `approx` — (a) the §11.11 live-calculator trades replayed through the fast path at variance 5; (b) `side_raw_adj` / `raw_adj_total` per package at both its own max and a dominating max; (c) real (my give, their get) package geometry; (d) a fuzz built to REACH the fallback arms, which the fleece bracket hides in normal operation and which the memo tables deliberately do not cover — the test asserts the fallback was actually exercised, else it proves nothing; (e) a cap change re-initialises the tables (`_RA` / `_ASSET` are keyed WITHOUT the cap for hot-path cost, so `_ensure` is what keeps that safe — the port's fallback arm genuinely runs at cap 10099 while a snapshot's top-up cap is `top_value + 80`, so two caps coexist in one process); (f) the M5 hoist precondition — `Package.v_sum` IS the float `sum(vals)` and `vals` ARE v-descending, for every package; (g) the whole leg pool is byte-identical, digest over every field of every leg.
17. **v8.2 pick-band swing pins:** (a) every future pick carries `mv_lo`/`mv_hi` = its round's literal Late/Early KTC tranches with `mv_lo < mv < mv_hi`, and every current-year pick carries `mv_lo == mv_hi == mv` (known slot ⇒ no swing, by construction); (b) the swing NETS by `(year, origin)` — ron's 2027 R2 out against his R4 in worst-cases at the GROUP figure (−258), never the group-blind adversarial sum (−498), because one season cannot resolve both ways — while independent origins compound: a same-year same-round pick swap is exactly ΔF 0 (§11.1b, untouched) yet swings ±(Early−Late) both ways; (c) **THE FIREWALL — the swing is a report, never a price**: prices (`p == mv == p_me`, flat Mid), the gate's adjusted totals, favor, the fleece ratio, the verdict, breakevens, the anchor ask, the deep-link tranche ids, the stored pools, the content fingerprint, and EVERY selection/retention/ordering key (walk keys, heaps, min-return bars, the §11.8b(e) doc sort) are byte-identical to the flat-Mid computation — pinned by `verdict_worst == False` rows surviving beside a True verdict (the disclosure v7.5 could not make without emptying the frontier), by stored `return_pct`/`return_view` equalling the neutral maximin figures, and by the doc order running on the neutral keys while the SHOWN floor/ceiling fold the swing; (d) mirror identity — the counterparty's extended floor uses the exact mirror of my swing (`min(ΔS_them, ΔF_them − up_me)`): one band, opposite signs, no seat premium; (e) degeneracy — a trade with no future pick has `down == up == 0` and the v4 interval verbatim; (f) the labels: swing surfaces always say "within KTC's published band" (the tranches are band averages, not hard bounds) and the per-origin audit line names the origin and both scenarios.
12. **v5 finder pins:** (a) `favor` derives from the SAME adjusted totals as the gate (one source of truth) and `|f| ≤ 5 ⟺` the port's `check_equality` at variance 5; (b) constraint soundness — no returned spread violates any require/exclude, pinned on the §4 example compilation (ronak/joey/Diggs/Burrow/Henderson) against fixtures; (c) δ-view identities — ranking by `return(δ)` at δ=0 and δ=1 equals ranking by ΔS-return and ΔF-return; robust mode reproduces v4 maximin exactly; (d) finder determinism — warm-cache and cold-cache results byte-identical; identical query → identical results; (e) honest coverage — exhaustively-crossed queries report exact counts, budget-saturated ones report floors, never estimates; (f) posture-compiled defaults reproduce v3.3 `posture_allows` behavior when no intel/query constraint names the team; (g) board buckets are favor_min bands and every stored pair still passes the §2 verdict.

## 12. The hedge database (v8)

Full design + decision record: `docs/hedge-db-v8-spec.md` (adversarially
reviewed, measured on the live snapshot before implementation). The CLI-only
(`core.scoring.hedgedb`, `core[hedgedb]` numpy extra — never the collector)
session engine behind the trade-negotiator skill: **complete legs, exact
searches, cached views.**

- **Why legs, not pairs:** the complete |favor| ≤ 5 pool measured 13,490,657
  legs on the live snapshot, and its ≥1%-floor crossing region ~2.5e12 pairs
  at a ~53% qualifying rate — the pair universe is not materializable at any
  dial. Every hedge is a pair of legs; the DB persists ALL of them, columnar
  (one `.npy` per column, package descriptors in asset order, ~2 GB), plus
  the FULL Snapshot it was priced from. Boards and offers rebuild their
  league from that stored snapshot — never live Mongo — so a page can never
  disagree with its own stored coordinates (the §11.8-style contract carried
  over). The v5 sampling caps do not exist on this path (drain mode).
- **Fingerprint:** content-normalized (Mongo `_id`/`fetched_at` stripped,
  lists content-sorted) and INCLUDES `ktc_calculator` — the DB persists
  priced values, so every pricing input keys it; `value_history_max` is
  excluded (dip notes only). Unchanged content ⇒ the build is a no-op.
- **Invariants (pinned in `test_hedgedb.py` at reduced params, brute-parity
  against the finder):** (a) `search` ≡ `find_spreads(favor_band)`
  bit-for-bit — spreads, counts, exact — on identical scopes, robust AND
  numeric-δ, including identical truncation at equal budgets; (b) cache hits
  byte-identical, keyed by COMPILED constraints + sliders (intel changes
  miss exactly when they should); (c) fingerprint invariance/motion; (d)
  stored-snapshot league prices identically to the live-built league; (e)
  per-team favor windows (`favor_for`) bind that team's legs only, and
  out-of-band values raise with the rebuild hint (never silently empty);
  (f) count-neutral offers stand alone, disclosed; (g) offer hedges offset
  the offer's signature exactly, exclude its counterparty, and the old ≤2
  -asset heuristic is gone — an empty complement signature is proof of
  absence at the DB's edges (measured live: (−1, −1) is EMPTY — no fair
  gate-clean 3-for-1 onto our side exists league-wide), and the favor
  window binds the HEDGE legs only (v8.0.1) — an already-received offer's
  own favor is disclosed, never a filter, so generous offers still hedge —
  and offers re-verdict on the RECEIVED basis (v8.0.2): band/fleece
  violations waive exactly when their excess flows to us, stay FAIL when
  we'd overpay, legality never waives, the original verdict + waived list
  ride the card as disclosure — and the offer VIEW is its own page
  (v8.1 pinned-offer mode): hedges come back grouped per counterparty
  (`by_team` — every non-offerer team present, top-K each, best pair
  floor return first, hedge-less teams trailing; `counts.matched_by_team`
  the crossing's full per-team tallies; the flat maximin list rides
  alongside, a superset of the old global top-K), rendered as the pinned-offer
  dashboard — the offer atop one card per remaining counterparty — at its
  own canonical out path beside the board's, which offer mode no longer
  touches;
  (h) legality is a
  persistent tri-state column, discovered by walks, max-merged across
  processes.
- **Honesty:** v5.1 language survives verbatim — `exact` ⟺ the walk
  completed (counts are tallies; empty means none exists inside the DB's
  edges), else verified floors; the warm-bar seed makes `matched` a floor
  (`bar_raised`) with the result list still exactly top-K. The DB's edges
  are §1's: band ±5, packages ≤ 3, top-2 protected, no taxi/unvalued gives,
  fleece bracket, (0,0) never pairs.
- **Measured (2026-08-03 live):** cold build ~14.5 min (pool 113 s,
  serialize ~3 min, 11-search bake ~9.5 min at 4 workers); cache hits
  0.03 s; 8/11 teams provably-exact top-50 (two teams provably ZERO fair
  hedges ≥ 1% — real answers), 3 saturate the 40M-visit budget (honest
  floors). League-wide favor-tightened novel queries are the slow shape
  (the geometry: tightening kills the edge-hugging top, the exact bar
  drops, the provable region explodes) — minutes, once per filter state,
  then cached.
- **v8.2 pick-band swing on DB surfaces:** search/offer rows carry the
  band-folded `floor`/`ceiling`/`return_robust` plus the `swing` block,
  computed at row-build time from the stored snapshot's packages — nothing
  persisted changes (no fingerprint move, no rebuild, caches stay valid),
  and retention/order stay on the neutral stored scalars (§11.17). The
  `_payload` docs and the pinned-offer entries are where the fold happens;
  `return_view` remains the neutral key the crossing's bar and sort ran on.

## 13. Changelog

- **v8.2 (2026-08-03):** the **pick-band swing** — the reported floor/ceiling fold KTC's published Early/Late band for future picks; the price stays flat Mid for every consumer. User: *"lets replace the current floor/ceiling metrics that exist in our hedge dashboards because those are redundant with the starter/face metrics"* — the old chips were `min/max(dS, dF)` rendered beside `dS` and `dF` themselves. Design chosen from an adversarially-judged study (owner-conditioned pricing, floor/ceiling report, variance metric, intel overrides): the report won on every lens because both recorded v7.5 failures ran through numbers the gate compares, and a report touches none of them.
  - **Mechanics (§2):** `Pick.mv_lo`/`mv_hi` = the literal Late/Early tranches (already loaded by `build_tranches`, never read for future years until now; current-year picks carry `mv_lo == mv_hi == mv`). `trades.pick_swing` nets by `(year, origin)` — one team finish drives all of an origin's rounds that year — and the reported interval becomes `[min(ΔS, ΔF + down), max(ΔS, ΔF + up)]`. Every trade surface carries a `swing` block: `down`/`up`, `verdict_worst` (does the §2 verdict survive the band's worst edge — `verdict_of` re-run as a disclosure, never the verdict), and per-origin audit lines ("2027 picks from ronakpatel32: −556 if they finish strong, +1280 if they crater").
  - **The firewall (§11.17):** prices, the gate, favor, the fleece ratio, verdicts, breakevens, anchor asks, deep-link tranche ids, the stored pools, the content fingerprint and every selection/retention/ordering key stay byte-identical flat Mid. `return_view` (finder/hedgedb) and the pair board's `return_pct` remain the NEUTRAL maximin keys; the shown `floor`/`ceiling`/`return_robust` fold the band, so a row's interval can sit slightly out of the displayed order — footer-disclosed. No hedgedb rebuild: the fingerprint hashes snapshot inputs, and no priced value moved.
  - **Surfaces:** hedge-board and pinned-offer rows keep the floor/ceiling/return chips (now band-folded — the redundant plain-maximin display is gone as asked) plus an expanded-row audit line; the pinned-offer card gains the accept-decision line ("if every unknown slot breaks against you… the verdict does/does not survive the whole band"); the CLI `score` card and `find` rows carry the same figures and audit line. Wording is always "worst within KTC's published band" — the tranches are band averages, not hard bounds (a true 1.01 beats Early).
  - **Worked examples re-pinned (§10):** §10.1's reported floor is −786 — numerically the exact loss v7.5 once CHARGED (same Late repricing), now a disclosure beside unchanged coordinates and gate; §10.2 keeps floor 0 (ΔS binds) with the ceiling extending 533 → 663; the Hunter conversion (§11.8b(b)) reports floor −206 (v7.5's number again) while its NEUTRAL floor 0 is what the stored universe still ranks on.
  - **Held for later, by design:** owner/rank_L-conditioned pricing (the twice-banned forecast — it also manufactures gate edge, §13 v7.4/v7.6 entries) and per-(origin, year) **declared-band intel overrides** (the only price-moving shape that can IMPROVE gate honesty once standings crystallize mid-season; would ride the posture_overrides channel and correctly force a rebuild — revisit if phase-1 usage shows repeated wants).

- **v7.6 (2026-08-02):** future picks price at **FLAT MID everywhere** — no forecast (v7.5's rule stands) and no direction premium (v7.5's rule retired, same session). User: *"i still think that we shouldnt be trying to estimate where future year picks are going to land inside the round, but instead of being pessimistic, lets assume that every pick will be in the middle of the round."*
  - **What changed vs v7.5:** `pessimistic_band` (Early when I send / Late when I receive) is deleted; every future pick prices at its round's Mid tranche whoever owns it — gate, ΔF, deep links, league tab, hedge board, all the same number. The price vector becomes SYMMETRIC between seats: a same-year same-round pick swap is exactly ΔF 0 again (v7.5 certified it a loss), the league tab's F column is a neutral survey again, and the §11.1b seat-asymmetry note inverts into a symmetry pin.
  - **What it trades away, stated in §2:** v7.5's pessimism only ever erred against me — a floor computed under it was a floor. The midpoint is neutral instead of cautious: a future pick that actually lands Early was under-charged on the way out. That is the user's chosen convention, considered against the pessimism explicitly; the objectivity framing in §2 now presents flat Mid as a stated zero-parameter convention rather than a worst case.
  - **Worked examples re-pinned a third time:** §10.1 is a preference trade again (ΔS −64, ΔF +78, δ* 0.4507; gate 24.0% REJECT — between the Early projection's 36.8% and the Late pessimism's 12.1% PASS) and §10.2 reads (0, +533) with the pre-v7 gate numbers, since a 2028 pick's flat Mid is numerically the old market band.
  - **Measured on the fixtures:** board 1,184 → **1,200** stored, buckets `[300, 300, 300, 284, 0]` → **`[300, 300, 300, 300, 0]`** — the [0,+5) counterparty-friendly bucket SATURATES for the first time, while [+5,∞) stays empty (v7.4's 184 pairs there rode the rank_L asymmetries; a symmetric vector offers no such wedge). **v7.4's headline pair returns number for number** (26.32%, ΔS +4,291 / ΔF +4,439.1 — its only pick is the current-year 1.01, which every lens priced identically; v7.5 had displaced it via the pool, not re-scored it). Every per-counterparty dashboard search is exhaustive again with the largest crossing at 10,358,086 — comfortably inside the 20M budget, which stays for headroom (§9).

- **v7.5 (2026-08-02):** future picks price at the **pessimistic tranche EVERYWHERE** — the projection is dead. User: *"we should NEVER try to estimate where a pick will land within a round in 2027 or 2028 or any future year. ALWAYS be pessimistic (assume that picks given away will be early in the round, assume that picks received will be late in the round). These should be pessimistic not only when finding hedges and finding trades, but also on our hedge board when displaying them."*
  - **What died:** the `rank_L` market band (next-year picks banded off the origin team's projected finish) and the flat-Mid rule (two years out). Both were estimates of where inside a round a pick lands, which is exactly what the ruling forbids. `band_of_rank_l` is deleted; `league.rank_l` survives for standings display only.
  - **What it means:** v7's two lenses collapse into ONE price per pick. Current year: KTC's numbered value (v7.4, unchanged). Future: Early if I own the pick (I would send it — the dear end), Late if anyone else does (I would receive it — the cheap end). The gate, favorability, the anti-fleece ratio, the return denominator, ΔF, the deep links, the league tab and the hedge board all read the same number — through v7.4 the gate and the links still argued from the projection while only ΔF was pessimistic, which is how a board row could show my 2028 R4 leaving as a "Mid 4th" (the user's bug report, twice: first the lens badge read as a display bug, then a received Late 2nd next to a sent Mid 4th read as an inconsistency — it was the projection leaking through every surface the pessimism didn't own).
  - **Deep links follow:** a future pick links the tranche id of its pessimistic band (mine → Early …, theirs → Late …), so the counterparty's page total still equals `adj_give`/`adj_get` by construction. `v_me`/`p_me` and every second-lens emitter survive as TRIPWIRES — they can no longer differ from `v`, and if they ever re-diverge the cards disclose it rather than hide it.
  - **Consequences priced in, not discovered:** (a) ΔF conservation is restored (`ΔF(them) = −ΔF(me)` exactly — the §11.1b mixed-lens wedge is gone with the second lens); (b) the §10.1 worked example's gate flips REJECT → PASS (7,799/8,868, gap 12.1%) because the get side's concentrated Early 7,398 became a Late 5,562 — gate-fair yet floor-negative for me is why the §2 verdict stays a separate hard constraint; (c) the league tab's F column is now MY seat's read (my picks Early, everyone's Late), which flatters exactly one row and says so in §7; (d) same-year same-round pick swaps remain certified losses under one pessimistic vector, as they were under the v7 lens.
  - **What it costs, measured on the fixtures, so it is a choice and not a discovery.** The board contracts 1,372 → **1,184** and the counterparty-friendly frontier pays almost all of it: [0,+5) 288 → 284, and **[+5,∞) 184 → 0 — empty again**, one release after v7.4 populated it for the first time. The mechanism runs through both sides of the gate at once (my future picks enter the counterparty's calculator dear, theirs enter cheap), so the stored intersection of "their calculator calls it a clear win for them" and "my floor clears 1%" vanishes at the far end. The top pair still improves, 26.32% → 26.61% — buys DeVonta Smith + Luther Burden off NoahMoell for my 2027 R1 + 2027 R4 both priced Early, and the buy STILL reads −9.2 favor. The broader fair band also grew every per-counterparty dashboard crossing (largest measured 16,110,117 — jaketoppen), which pushed one team past the 16M finder budget; `finder_cross_budget` moves to **20M** so the per-counterparty searches stay exhaustive (§9).

- **v7.4 (2026-08-02):** current-year picks price at **KTC's real numbered value**, for the gate and the ledger alike. User, correcting me: *"i dont care to see the estimated 2026 tranche at all for every pick. this shouldnt even be included in the trade calculation. for all 2026 picks, the exact number pick should be used for the gate and for trade calculations because we know it"*, then *"just use the real KTC value"*.
  - **v7.0 misread the instruction, and this records why.** The user's original brief said plainly that KTC prices picks per-pick in the current year. I checked the scraped 500-asset payload, found only the 36 generic tranches, and concluded KTC published no such price. The payload is not the calculator: `startTradeCalculatorPage()` calls `addNumberedPicks()`, which GENERATES the 48 numbered records client-side. Two errors followed. "Use the exact number" became the rookie board's n-th-player value — a PROXY, and one that missed in both directions (7,762 vs KTC's 7,897 on a 1.01; 2,927 vs 2,821 on a 3.03). And because I believed no per-pick price existed, I carved the GATE out of the instruction and built a two-lens split for current-year picks that was never asked for.
  - **The symptom, and the proof it was the prices and not the gate.** Our gate read 9,415 / 10,069 on a leg where the live page showed 9,421 / 10,198. Solving backwards, the (already integer-exact) gate reproduces the live figures EXACTLY once the two picks are priced 2,898 / 2,160 instead of their tranches 2,833 / 2,022. The port was never wrong; it was fed the wrong pick prices.
  - **`core/scoring/ktc_picks.py`** ports KTC's generator (`calcPicksRookies`, the `LEAGUEYEARPHASE == 2` branch), pinned **48/48 exact in both the 1QB and superflex columns** against `data/fixtures/ktc/tc_capture_2026-08-02.json` — a one-shot capture of `rawPlayers` off the running page, so its 500 inputs and 48 `calculated: true` outputs are self-consistent, which is the only way to pin a board that moves daily. An independent verifier reproduced all 48 by running KTC's verbatim minified JS under node and differentially fuzzing it against the port. Against our own nightly scrape (5 h older) the worst slot drifts 1.28% — board movement, not port error.
  - **One number per current-year pick:** `p == mv == p_me`. The two-lens split survives ONLY for future years, which is exactly where the slot is genuinely unknown and where the user asked for pessimism. This also kills the manufactured-ΔF hazard v7.0 shipped (six opponent picks priced above market on my side): there is no longer any gap between my price and the market's.
  - **Two of the 48 are fractional and we keep the fraction.** KTC derives 1.01/1.02 off the rookie ladder rather than a tranche and passes the unrounded figure into its own adjustment; rounding would put us off their number by more than it would buy. §11.13f/XTOL's integrality premise gains exactly two documented exceptions, both KTC's, both absorbed by `XTOL = 1e-6`.
  - **Deep links inverted.** A current-year pick now links at its NUMBERED id (202611) because that is what the gate prices; a future pick still links its tranche. Through v7.3 it was the other way round, with the numbered id merely DISCLOSED — backwards, and the direct cause of the screen disagreement. `Link.numbered` / `numbered_url` are now empty: one price, one link, nothing to disclose.
  - **It gives the board back, and then some.** v7.0's proxy cost the fixture board 1,178 → 925 pairs and gutted the counterparty-friendly end ([0,+5) from 282 to 89). Real prices take it to **1,372**, buckets `[300, 300, 236, 89, 0]` → **`[300, 300, 300, 288, 184]`** — and **`[+5,∞)` is non-empty for the first time (184)**, which falsifies the standing claim (v5, restated at v7.0) that a pair BOTH counterparties' calculators like while I keep a positive guaranteed floor essentially cannot exist. The mechanism is that the two axes moved by very different amounts: a 1.01 gained **+1,752 on the gate** (6,243 → 7,994.86) but only **+233 on my ledger** (7,762 → 7,994.86), so shipping a current-year pick now buys far more from the counterparty's calculator at almost no extra cost to my own floor. That asymmetry is not a modelling choice — it is what happens when a price stops being a proxy.
  - **Refuses rather than falls back.** `LEAGUEYEARPHASE` / `DRAFTYEAR` are not in the values payload (they live in `site.min.js`), so `core.ktc.fetch_calculator_globals()` scrapes them — two GETs, no browser — the collector stores them as a `ktc-calc` singleton, and `Snapshot.ktc_calculator` carries them. Missing or wrong-phase means current-year picks have NO price and `build_league` alerts: reverting to the generic tranche is the exact silent failure this entry exists to remove.

- **v7.2 (2026-08-02):** the **hedge board** — the dashboard that replaced the tab. User: *"when I use the trade negotiator skill, I want the skill to generate an on the fly html dashboard showing the best hedge options for each counterparty."* `core/dashboard.py` is two pure functions — `hedge_payload` (one `find_spreads` per counterparty) and `render_html` (a standalone page: no scripts, no external requests, `details/summary` disclosures) — behind `scripts/score_trade.py dashboard` / `just hedges`; the skill runs it at session start and publishes the file as an artifact. **One search per counterparty, not one global top-N**, because a global ranking is dominated by whichever two teams happen to match up best and the desk needs a read on EVERY team — including the ones with nothing, which is itself an answer. `with_team` pushes the scope into the crossing (§4a v6), so each run is exhaustive rather than budget-truncated: on the committed fixtures all eleven report `exact: true`, ~26 s each, 2.6 M qualifying hedges in the fair window. The pool build (~15 s of each) is identical across the eleven — same constraints, same opponent slate, since `with_team` constrains the PAIR and not the enumeration — so the runs are farmed to a fork pool rather than refactoring the heavily-pinned `find_spreads`. The page re-derives NOTHING: same call, same leg cards, same honest counts, so it cannot disagree with `find`. Visual contract is `docs/web-design.md` verbatim (Chicago-flag palette, mono numerals, red only ever a negative number or the star), with two deliberate departures — dark tokens are added because an artifact renders in the viewer's theme, and **favor is rendered flat with its direction in words** rather than in ledger-red, because a negative favor is GOOD for us and the ledger convention would say the opposite.

- **v7.3 (2026-08-02):** KTC trade-calculator **deep links** on the hedge board, per leg and per spread. User: *"can we supply links to how the trade looks in KTC? both on a per-leg basis and on a total spread basis."* `core/scoring/ktc_link.py` (built earlier on a side branch, cherry-picked here — 16 pins, all green under the v7 lens) resolves every asset to its KTC playerID and emits a link pinned to the settings the §3.1 gate ports: `var=5`, `pickVal=0`, `format=1` (load-bearing — without it the site falls back to a Superflex cookie and every value on the page is the wrong column), `isStartup=0`, `tep=0`. Convention `teamOne` = what I SEND, which is what makes a leg's page total exactly `gate.adj_give` / `adj_get`. **The per-leg links are the real ones** — a leg IS a trade with one counterparty. **The spread link is a what-if and is labeled as one on the page**: nobody executes it that way (the legs go to two different managers) and KTC's adjustment does not cancel across a pair (§3.1 v3.4), so its fairness reading is its own, not the sum of the legs'. Disclosures rather than silence: an asset with no KTC identity is dropped and named, a side that empties out yields NO link instead of a one-sided giveaway, KTC's 12-asset cap can block the combined link (stated, not hidden), and where a current-year pick also exists as a numbered KTC entry priced above the tranche the gate used, a second link shows that version and says its total will differ. **Picks link at their TRANCHE**, matching `Asset.v` — v7's `v_me` is my own book and has no counterpart on their screen; a pick's band follows its ORIGIN team, not its holder.

- **v7.1 (2026-08-02):** the Trades tab is gone, and the pair board left the nightly pipeline with it. User: *"lets also remove the trades tab and just focus on the cli and dashboard generation for finding trades."* Removed: the `/trades` route and its four components, the trade-recs types and `getTradeRecs` in `apps/web/src/lib/queries.ts`, the nav entry, and `trades.spec.ts`. `compute_all` no longer calls `trades.trade_board`, `store.scoring_docs` no longer emits a `trade-recs` document (two singleton collections now, not three), and the collector's trade counters/log lines are gone. **`trades.py` itself is untouched** — `scripts/score_trade.py` calls `trade_board` directly and never read the stored doc, so the CLI and the trade-negotiator skill are unaffected. The §5/§11 invariants are statements about the engine rather than about the collector's output shape, so they keep every pin: a session-scoped `board` fixture builds `trade_board(league)` the same way the CLI does. `test_runtime_within_budget` split accordingly — the nightly path gets a tight 20 s bound (it is now the cheap half) and the board keeps the old 90 s bound, measured separately; `test_determinism_byte_for_byte` now asserts byte-identity for BOTH, since the board is the half where determinism is hard (heaps, walk order, tie-breaks).

- **v7.0 (2026-08-02):** draft picks got a SECOND price, and `ΔF` reads it. User: *"For any picks that we trade in the current year, use the exact number because we can calculate it by using the actual draft order. For trades that involve picks in future years, lets be pessimistic. Assume that any pick we give up will be a pick early in the round, and any pick that we receive will be a pick late in the round."*
  - **Two lenses, one fork point.** `Asset.v` / `Package.v_sum` stay MARKET everywhere they were market: the reverse-engineered KTC gate, favorability, the 1.35 anti-fleece ratio, the pricing bracket, the anchor ask, `market_return_pct`, and the return DENOMINATOR (`L_SENT`). The new `Asset.v_me` / `Package.v_me_sum` feed exactly one thing — `ΔF` on MY side. `ΔS` cannot be affected at all: `lineup.pos_columns` drops picks, so no pick has ever entered the starter solve.
  - **Current year = EXACTNESS, not pessimism.** The draft order is known (`slot_to_roster_id`, linear draft), so the slot is known: price the pick at `board_value(12·(rnd−1) + slot)`, rounded. This was already computed and labelled "information only; never scored" — v7 scores it. Be precise about what it is: board-vs-tranche error, and it points BOTH ways. On the fixture my 1.01 books 7,762 against a 6,243 tranche (+24%) while my 2.09 books 3,236 against 3,504 (−8%); six opponent-owned current-year picks price ABOVE market for me (+1,070 total), so acquiring one can book `ΔF` the market does not offer. That is the requested reading of "the exact number", but it is an arbitrage signal, not a safety property.
  - **Future years = PESSIMISM, keyed on ownership.** A pick I own is one I would send ⇒ **Early** (the dear end); a pick the counterparty owns is one I would receive ⇒ **Late** (the cheap end). Ownership fixes the direction because every leg is me ↔ one counterparty and the owner is always the sender, so `v_me` is a single static per-asset vector fixed at snapshot build — no asset is ever priced two ways inside one pool. Here `p_me ≤ mv` on acquisition and `≥ mv` on disposal, always. The `rank_L` band survives as the MARKET band on `mv`; it no longer sets what I pay.
  - **What this costs, measured, so it is a choice and not a discovery.** The fixture board goes **1,178 → 925 pairs** and the favor buckets **[300, 300, 296, 282, 0] → [300, 300, 236, 89, 0]** — the `[0, +5)` bucket, the counterparty-friendly frontier v6 fought a search bug to populate 0 → 282, falls to **89**. Cause is breadth, not extremes: 78.9% of my give packages carry a pick and 73.9% of opponent get packages re-price, so the modal leg loses ~1,000 of `ΔF` (≈ 11 pts of return on a ~9,200 denominator) and a canonical buy+sell pair takes the hit on both legs. Two consequences worth stating out loud: (a) a same-year same-round future-pick swap is now a certified loss (my 2028 1st for theirs = `ΔF −886`) and can never be stored; (b) all eleven opponent-owned 2027 1sts price at 5,562 on my side, so `ΔF` no longer ranks WHICH first to target — buying the projected 1.01 off a tanking team scores −1,836 while the market charges +1,280 for it. A one-band ratchet (Early→Mid on receive rather than Early→Late) was measured as the alternative: it restores that ordering but recovers **none** of the board (still 925 pairs), so it trades strictly less pessimism for no breadth.
  - **One lens everywhere was considered and REJECTED.** It is the cheaper diff — §11.1 survives verbatim, `total_face` stays a state function, and `Asset.v_me` / `Package.v_me_sum` / `Pick.p_me` / the `lens=` threading all disappear. It is wrong because the gate is a *verified port of an external calculator* whose asset universe contains exactly 36 generic RDP tranches and no numbered pick: feed it `v_me` and "FAIR" stops predicting what the counterparty's screen shows, taking the favorability slider, the anti-fleece ratio, and the anchor ask with it. Ratifying precedent is v3.4's own split — "Face KTC remains the market-pricing layer (gate + return denominator); the ledger is what we optimize."
  - **§11.1 split, and what actually survives.** Market-face conservation is exact and untouched (invariant 1). The my-lens ΔF is *also* exactly conserved per leg — `v_me` is a global price vector — so what changed is only that the card REPORTS my side through `v_me` and theirs through `mv`, and the two printed numbers therefore need not negate (invariant 1b). That is a presentation choice, reversible. Every search bound rests on **additivity across legs on one side**, which is untouched: `ΔF_pair = ΔF_buy + ΔF_sell` exactly, `floor ≤ ΔF` definitionally (0 violations over all 492,708 fixture pool legs), so the v5.1 sound crossing bound and the `k5 = 3·ΔF + 2·A` walk key stay sound with `ΔF^me` substituted. `total_face` now takes a REQUIRED `lens=`, because the my-lens answer on an opponent's roster comes back as `−ΔF(me)` and would look exactly like conservation proving itself.
  - **Integrality kept structural, not accidental.** `board_value` interpolates missing ranks and returns a float; three separate written arguments (the integer `k5` coefficients, `XTOL`'s reachable-tie rationale, §11.13f's no-tolerance bound) assume the coordinates are integral. Today the board is gapless over 1..48 so nothing interpolates — an accident of this snapshot. `price_pick` now rounds.
  - Also swept: `Pick.sell_floor` and `Pick.pricing()` deleted (both unreferenced, and `sell_floor = max(p, mv)` was a second competing asymmetric price); the `concrete` annotation dropped from trade cards (`v_me` supersedes it — identical number on current-year picks); collector Lambda unchanged.

- **v6.0 (2026-08-01):** the search stopped skipping a whole family of spreads, and the gate stopped recomputing itself.
  - **The Δplayers == 0 crossing family was unreachable.** Every walk opened with `if sig[0] <= 0: continue`. A spread's legs have complementary count-signatures summing to (0,0), so bucket `sig` crosses `−sig` — and that test rejects BOTH sides of the Δplayers == 0 family. On the live pool **93,165 of 348,223 legs (26.8%)** sat in those buckets, **41,722** of them in (0,±1)/(0,±2), which are genuine hedges: swap players evenly with two teams while netting a pick from one to the other. 169M crossings, +6.1% of the space, invisible since v3.3. Replaced by `canonical_sig` at all seven sites plus `pair_in_space`, the recommendations/watch split, and `leg_type` (a leg netting 0 players but ±k picks was labelled "neutral" — it is not executable alone and it is now pairable). **(0,0) stays deliberately non-canonical:** such a leg is already roster-neutral by itself, so pairing two is two unrelated even swaps, and 51,443 of them would mean ~1.3e9 pairings of noise. Verified newly-reachable pair: buy 2026 1.01 + 2026 3.03 → 2026 2.02 + 2027 R2 + 2028 R2 from vishan (favor −4.90), sell Godwin + Flacco + 2027 R1 → Zay Flowers + Jameson Williams to ronak (favor −5.00) — floor 2,232, return 11.32%, both legs gate-clean, net 0/0.
  - **A coverage fix under a fixed budget makes the board WORSE, so the budget moved too.** The same 400,000 collection visits spread over a larger space store fewer pairs, not more — measured live: 400k → 619 stored / 19 both-legs-fair, 1.6M → 1,101 / 478, 4M → 1,133 / 528. `pair_collect_budget` 400,000 → 4,000,000 (61.0 s, 236 MB against the collector's 900 s / 2048 MB). On the fixture the board goes 886 → 1,178 with the headline pair unchanged, and **the [0,+5) favor bucket goes 0 → 282 of its 300 quota**. v5 recorded that bucket as empty and reasoned that trades both counterparties' calculators like, while I keep a positive guaranteed floor, essentially do not exist. That reasoning was wrong: it was empty because the search never looked. The geometry argument survives only for [+5,∞), which is still empty.
  - **A SOUND bound on the pair's joint ΔS (§11.13(f))** — the open correctness gap since v5.1. Each leg stores `A`, its add-only starter gain: what its RECEIVED package alone would add to the current lineup, nothing sent away. It is a function of the get package only (picks cannot start, so `pos_columns` drops them), hence one solve per distinct package. Starter sum is the max-weight basis of a transversal matroid, so it is monotone and submodular: removals only lower it and additions are subadditive, giving `ΔS_pair ≤ A_buy + A_sell`. Audited **0 violations over 24.4M crossings with no tolerance**, where the naive isolation-ΔS sum violates 26.6% — which is the v5.1 bug restated, so the pin asserts the fixture still exhibits it. With ΔF exactly additive, the pair floor now has a fully separable bound `floor ≤ min(A_b + A_s, ΔF_b + ΔF_s)`, strictly tighter than v5.1's `ΔS ≤ Σ face received`. **Note for anyone tempted by a blended key:** use integers. The float blend `0.6·ΔF + 0.4·A` has a real one-ULP counterexample (a pair with ΔS = ΔF = A_sum = 748 whose blended bound is 747.9999999999999, dropped at K=1266); `3·ΔF + 2·A` against `5·θ` is the same ordering with no rounding.
  - **`with_team` is scoped before the walk, not after it.** The test sat in the innermost loop AFTER `visits += 1`, so a per-counterparty query walked the league-wide space, charged its whole budget to it, and then discarded everything off-team. Legs in a pair always have distinct counterparties, so "buy leg is theirs" / "sell leg is theirs" are mutually exclusive and jointly cover every in-scope pair — the scope is a PARTITION and pushes onto the sell list per buy leg, leaving both orderings (and so every truncation) intact. Live effect: the fair-window queries for ronak / DrewR87 / jaketoppen now complete **exhaustively** at 3.4–5.6M crossings (`exact=True`) where they saturated the 16M budget before.
  - **The gate stopped recomputing itself (§11.14).** `reverse_adjust` is called at two sites. At the TOP-UP site its argument tuple is fully discrete — `floor(|e−a|)` is an int, `r_max` an integer KTC value, the nerf index 0..3, the cap a snapshot constant — so an exact-key dict collapses 1,850,522 calls to 77,700. The FALLBACK site is left alone: 7.4% hit rate on float keys, and quantising breaks exactness long before it buys reuse (6 significant digits already mismatches on 983 of 559,718). Three more exact-key tables (`o` on `r_max`: 2,455,184 → 201; the seed on `(a,t)`: → 367; the per-asset term on `(v,r_max,nerf)`: 5,898,935 → 10,177) plus the branch merge that skips both `check_equality` calls on the 73.8% of calls where the sign already decides the arm. `ktc_adjust` stays the pristine port; `ktc_fast` earns its keep by agreeing BIT FOR BIT — §11.14 compares `repr()`, never `approx`. Measured: gate 2.939 → 0.631 µs/call (4.66×), pool build 14.71 → 7.89 s (1.86×), whole-pool digest `ec6314bd3b8308c4` unchanged.
  - **The complete fair-band generator exists but is OFF by default.** `build_pair_pool(favor_band=β)` drains the bracket with no `variant_scan_cap` / `variants_per_signature`, screened on that band's own much tighter bound (`b* = T_max·2β/(100−β)`, `screen_slack` 1.15 against a measured minimum of 1.0308). On the live snapshot it yields **11,911,255 legs**, contains **all 53,883** fair legs the capped scan finds (zero missing), and moves the pool favor median **−9.40 → +0.10** — the Σv-descending sampling bias, which systematically under-represented exactly the counterparty-friendly end a spread needs, is gone. It stays opt-in because it **MEASURES 3,904 MB** as Python tuples holding `Package` objects (~330 B/leg). The design study's 479 MB figure was for a columnar layout that was never integrated with the walks, the sinks or `pair_eval`. **Columnar legs are the prerequisite for making this the default**, and until then `pool_favor_band = None` keeps v5 output byte-identical.
  - **The finder's crossing walk had no prune at all.** It sorted by a yield heuristic and walked a flat nested loop until the budget ran out — survivable on a sampled pool, hopeless on a complete one. It now carries the sound break, keyed on `k5 = 3·ΔF + 2·A` against `5·bar·Σsent` (integer coefficients, per the ULP note above). **ΔF alone is sound but a poor ORDER** — it ranks by face gained, so high-face/low-starter crossings flood the front of a truncated walk: with ΔF only, the market-constrained query got WORSE on the complete pool (12.75% → 5.38%); with `A` in the key it recovers to 12.75% and completes exactly at 11.3M crossings. The prune pays on the sampled pool too — crossings 3.7M → 57k, 417k → 23k, 94k → 1.1k, identical results.
  - **The warm bar, and what it costs.** The break only bites when the bar is high, and `min_return` defaults to 0 where it degenerates to "ΔF_pair ≥ 0". A seed pass crosses the richest few hundred legs per side and takes the K-th best return as the bar. This is EXACT, not heuristic: every seed value is a return a real qualifying pair achieves, so the true K-th best is ≥ it, and the seed shares its filter chain with the main walk. But it makes the RESULT LIST exact while turning `counts.matched` into a floor — it can only count pairs at or above the bar it walked. §11.12(e) pins those counts as honest, so **the seed runs only on the complete-pool path**, where the walk is budget-truncated anyway; the sampled path keeps exact v5 count semantics. (`test_honest_counts_exact_vs_floor_11_12e` caught this — matched 102,868 → 27,511 — before it shipped.) Relatedly, `counts.valid` is now `≤` a brute-force count rather than `==` it: the walk legitimately visits fewer crossings. `matched` stays pinned EQUAL, which is the property the break's soundness actually asserts.
  - **The complete pool is a PER-QUERY field, not a param.** `favor_band` bounds what the pool can REPRESENT: a band-5 pool cannot answer a `favor_min = −10` query at all, so defaulting it on would silently break slider 3's own presets. The guard raises rather than quietly returning nothing. Measured live against the sampled pool: ronak floor 2,232 → 3,098 and return 14.60% → 16.09%; a two-named-legs query 1,378 / 11.81% → 1,719 / 15.16%. Cost is 30–120 s and ~4 GB — fine for the local interactive path, which is exactly why the board does not use it.
  - **Still open:** the per-cell exact top-K walk — the real fix for budget-truncated counts on the BOARD, which still runs the sampled scan with a 10× collection budget. It needs the complete pool to fit the collector, which needs columnar legs.

- **v5.1 (2026-08-01):** the pair-crossing prune became SOUND for the floor/robust objective. v5.0.1 fixed *where* the walk looks for δ-views but left the floor objective ordering and pruning on each leg's ISOLATION floor, which implicitly treats the pair floor as additive. It is not — ΔS is jointly determined by both legs through the lineup — so pairs whose legs are individually weak but complementary were pruned unseen: a robust favor-banded query returned **0** where an exhaustive crossing of the same state held 4,157,460 verdict-true pairs, 88,911 clearing its own 1% bar (best 3.00%, floor +439; its buy leg shipped a starter, scoring terribly alone). Fix: key the walk on `ΔF − r·Σsent`, which is exactly additive and bounds the floor from above, so the prune can never discard a qualifying pair. Completed floor-objective walks are now EXACT (provable counts; "no spread matched" means none exists); truncated ones remain verified floors. Affects robust queries and the nightly board; δ-views keep the disclosed heuristic pending runtime measurement. Prompted by the user's challenge to the "market dries up" readings: "every trade that we make is roster-neutral… new trade opportunities should be popping up as our roster changes."
- **v5.0.1 (2026-07-31):** finder search-coverage fix. The v5.0 walk crossed the FULL pool in isolation-floor order under the crossing budget, but a favorability window concentrates the qualifying pairs exactly where isolation floors are small or NEGATIVE (fair legs have tiny face edges; floor = min(ΔS, ΔF)) — a δ-view favor-banded query returned 0 while an exhaustive check of the same pool found 3.1M qualifying pairs: the floor-ordered walk pruned the fair region by budget exhaustion, not by any verdict. Fix: (a) favor bounds push down to leg level before the crossing (exact decomposition — favor is per-leg), so favor-banded queries typically finish exhaustively and report exact counts (with a window set, the reported leg counts are the favor-eligible legs); (b) numeric-δ queries order the walk by the per-leg δ-score `leg_ΔW(δ)` instead of the isolation floor, so truncation fronts what the view ranks — non-certifiability doctrine unchanged; (c) the CLI/skill "no spreads" message now states whether the constrained space was exhausted (exact) or budget-truncated (a floor, not proof of absence). Robust queries and the nightly board path are byte-identical to v5.0.
- **v5.0 (2026-07-29):** the spread FINDER: constrained search over the full pair space with three sliders, driven by the trade-negotiator skill. (a) **Counterparty favorability** — per leg, the signed KTC-adjusted edge to the counterparty in KTC's own variance units (same adjusted totals as the gate; |f| ≤ 5 = their calculator literally says FAIR), pair favorability = min over legs, slider with presets; replaces the v3.4.1 raw-skim leg cap on the board (raw skim diverges from the calculator's number by up to 14 pts). User: "a leg looks beneficial to the counterparty if KTC would call that trade a fair trade or a trade that skews in their favor… I want some sort of metric where I can adjust how much the legs skew in my counterparties' favor." (b) **Market constraints as one vocabulary** (§4): posture defaults, market-intel, and ad-hoc query constraints compile to the same require/exclude/prefer predicates with strict precedence; the user's examples (ronak wants picks, joey wants a RB, nobody wants Diggs, josh b wants Burrow, colin shopping Henderson) are the pinned compilation tests. (c) **The finder** (§4a): disk-cached per-snapshot leg tables, constraint push-down before gate work, exact combined coordinates, budgeted crossings with verified-floor honesty, `find` CLI + skill protocol; constraint-adding and slider moves are cheap by construction. (d) **δ returns as a SLIDER, not a parameter** — `ΔW(δ)` is evaluated per query from the stored v4 coordinates; robust (all-δ) stays the default and the objective verdict is unchanged. User: "I want to be able to efficiently filter and sort these found spreads by the following 3 sliders."
- **v4.0 (2026-07-28):** the blended scalar died; the score became two objective coordinates. From-scratch re-derivation at the user's request ("forget everything… I want to know 'Will this trade make my team better and by how much?' … an objective calculation that works in all cases", with explicit pushback invited on starter-vs-backup and on objectivity). Result: every single-number ledger hides a stored-value preference δ ∈ [0,1] (a time preference, bounded by free disposal below and the fielding option above — not measurable, as the user's own QB example proves: a rebuilder should TAKE the trade the user correctly refuses). v4 therefore reports the two δ-endpoints themselves: ΔS (starter value) and ΔF (face owned). Verdict = both ≥ 0 (the user's strategy sentence, formalized); magnitude = the interval between them, floor guaranteed; ranking = maximin on the floor; preference trades get a derived per-trade breakeven δ* instead of a verdict. v3.5's δ = 0.25 is retired — the framework the user approved with "yes this is great, lets go with that." Starter-vs-backup survived the invited pushback as the ΔS coordinate itself (deployment-dependent value is the source of the arbitrage); the honest residuals are that ΔS measures market value of the deployable nine (not projected points) and that "how much" is irreducibly an interval.
- **v3.5 (2026-07-28):** bench players and picks were unified into one **stored-value** class counted at `δ = 0.25` of face, replacing v3.4's split of bench = 0 / picks = 100%. That seam was farmable: selling a non-starter for a pick of equal value paid ≈ +4,116 of pure reclassification and produced the board's 40%-return pairs, while count-neutrality was the only thing fencing it in. The fix came from the user's two QB cases — "I can only start one QB" (8,000 + 4,000 → 7,000 + 6,000 must be bad) and "a trade that upgrades my bench without decreasing my starters or my picks can still be a good trade" (8,000 + 5,000 → 8,000 + 6,000 must be good) — which bracket δ to (0, 0.5); the midpoint was chosen. Pure deployability (δ = 0) was rejected because it scored the second case at exactly 0.
- **v3.4.1 (2026-07-27):** the board filter's max dial changed dimensions — from a cap on total pair return to a cap on EACH leg's market return (face skim off that counterparty), with the list always sorted by total return desc; storage re-stratified by max-leg bucket with a bucket × total-band honest count grid. User: "filter based on max return for individual trades in a pair. If I set a cap at 5%, this means that the individual return for either of the two legs cannot be greater than 5%, however I want to see these in descending order by total return (not per-leg return)."
- **v3.4 (2026-07-27):** (a) The wealth ledger became STARTER KTC + PICKS KTC: `W = S + P` with `S` the max-Σv starting lineup at raw KTC over active + taxi (bench/IR = 0) and `P` picks at tranche; `ΔW` is per-side and zero-sum is retired (both ledgers can gain — that IS the arbitrage); pair `ΔW` is computed on the combined legs; the per-leg return floor is retired (buy legs are legitimately negative alone). User: "as long as our total STARTER KTC / FUTURE PICKS KTC is increasing, we dont care how that wealth is distributed (studs / lots of solid players). Lets make sure that our trade logic encodes this." (b) The fairness gate's fitted consolidation coefficients were replaced by the exact reverse-engineered KTC calculator adjustment (13/13 live calculator trades integer-exact; the adjustment provably does not cancel across roster-neutral pairs — user: "Value adjustments dont cancel out"). Face KTC remains the market-pricing layer (gate + return denominator); the ledger is what we optimize. Measured at implementation: the exact gate is materially STRICTER than the fitted band it replaces on asymmetric-shape trades (both of §10's old worked examples now fail it — KTC's concentration premium is real and large), and because the pair walk must order legs by their isolation ΔWs while pricing pairs by the non-additive combined ledger, every band count became a verified floor (§5).
- **v1 (2026-07-26):** ω-blended lineup+wealth+crunch score. Superseded: crunch mis-modeled as a per-transaction cost; recommendations were sell-only.
- **v2 (2026-07-27, never implemented):** wealth arbitrage with probabilistic inventory-risk (clearing odds, hazard decay, four-outcome pair EV). Superseded the same day: the probabilities were not computable from available information ("I want the parameters and inputs to come from information that you can reliably and accurately compute").
- **v3.3.1 (2026-07-27):** the min-only return dial became a min/max RANGE over the same presets, and pair storage went stratified — top-100 per return band with per-band honest counts (`bands`) — because top-500-overall storage couldn't serve a range query (all 500 stored pairs sat at the very top of the space). User: "filter... so that i only see the trades within a certain return range."
- **v3.3 (2026-07-27):** prune-then-pair inverted to enumerate-then-filter — full legal pair space computed nightly, user-supplied target return (UI dial; skill takes the same dial with qualitative constraints), posture promoted from annotation to engine constraint (BUYER receives players / SELLER receives picks / NEUTRAL either). User: "our pair and prune logic is severely broken… compute every possible legal trade pair that nets a KTC with that return."
- **v3.2 (2026-07-27):** neutrality extended to draft capital — a recommended pair nets exactly zero players AND zero picks for my side (user: "this does not account for draft picks… we should end with the same amount of picks and same amount of players as before"); unpaired legs demoted to building blocks.
- **v3.1 (2026-07-27):** proposal policy inverted after first live board review — minimum in-band gap clearing `W_min` instead of band-edge maximization (the band is tolerance, not target), and the board unit is strictly the hedged pair (buy side + sell side); standalone buys never surface. User: "every trade should come with a corresponding hedge… the model is giving me extremely lopsided roster-neutral trades that nobody would ever take."
- **v3 (original):** pure face-value KTC arbitrage. One score (`ΔW`), one gate (observed KTC fairness), qualitative targeting (observed posture + visible holes), roster-neutral pairings, execution protocol as rules. Successive user simplifications removed: counterparty utility simulation ("to determine if they would think the offer is fair we should just use pure KTC math"), roster-count/cut signals ("that does not mean that they are a seller"), and the incoming cut-fodder gate (cancelled — face value both directions, no exceptions).

## Design provenance

v3 was specified directly in conversation with the league owner (2026-07-27), distilling the v2 judged-panel work (four lenses, three verifying judges) down to the subset whose inputs are reliably computable. The v2 panel's durable contributions that survive in v3: the empirically measured fairness norms, the posture-based targeting thesis, the agreement-first/hedge-first execution protocol, and the demand-honesty and exclusive-with card annotations (as qualitative notes).
